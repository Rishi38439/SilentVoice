name: silent_voice
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^6.2.1  
  supabase_flutter: ^2.0.0
  camera: ^0.11.1
  video_player: ^2.5.0
  provider: ^6.0.5
  shared_preferences: ^2.5.2
  image: ^4.5.4
  cupertino_icons: ^1.0.8

  firebase_core: ^2.30.0
  firebase_ml_model_downloader: ^0.2.4
  google_mlkit_object_detection: ^0.10.0 # ⬅️ Downgraded to avoid conflict
  google_mlkit_image_labeling: ^0.9.0
  google_mlkit_text_recognition: ^0.9.0
  path_provider: ^2.1.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/app_logo.png
